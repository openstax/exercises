class ExerciseEditor.LogicLibraryListView extends Marionette.ItemView
  template: "logic_library_list"

  tagName: "div"
  # className: "logic-library-list"

  # ui:

  # events:
    
  initialize: () ->
    # @listenTo @model, 'change:currentLogicOutputIndex', (model) => 
    #   @ui.currentPermutationInput.val(model.get('currentLogicOutputIndex'))
    #   @model.trigger('change')
    # @parent = @options.parent

