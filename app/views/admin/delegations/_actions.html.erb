<%#
  Clients of this partial must provide the following variable:
  user
%>

<%=
  link_to 'Delegator', '#', data: { turbolinks: false }, onclick: <<~ONCLICK
    $('#delegator-id').val('#{user.id}');
    $('#delegator-type').val('#{user.class.name}');
    $('#delegator-name').val('#{user.name}');
  ONCLICK
%>

<br>

<%= link_to 'Delegate', '#',
    data: { turbolinks: false },
    onclick: "$('#delegate-id').val('#{user.id}'); $('#delegate-name').val('#{user.name}');" %>
