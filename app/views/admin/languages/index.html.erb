<h1>Logic Languages</h1>

<%= render partial: 'index', locals: {languages: @languages,
                                      action_partial: 'admin/languages/actions'} %>

<br>

<%= link_to(new_admin_language_url) do
      '<span class="glyphicon glyphicon-plus">Add</span>'.html_safe
    end %>
