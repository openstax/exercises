<%= page_heading 'Add user' %>

<%= render :partial => 'shared/users_search',
           :locals => { :current_path => new_user_group_user_group_user_path(@user_group),
                        :container => @user_group,
                        :presence_check_method => 'has_user?',
                        :already_present_text => 'Already in group',
                        :form_action_path => user_group_user_group_users_path(@user_group) } %>

<br/>

<%= link_to 'Back', user_group_path(@user_group) %>
