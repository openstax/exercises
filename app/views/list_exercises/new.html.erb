<%= page_heading 'Add exercise' %>

<%= render :partial => 'shared/exercises_search', 
           :locals => { :current_path => new_list_list_exercise_path(@list),
                        :exercise_types => 'published exercises',
                        :container => @list,
                        :presence_check_method => 'has_exercise?',
                        :already_present_text => 'Already in list',
                        :form_action_path => list_list_exercises_path(@list) } %>

<br/>

<%= link_to 'Back', @list %>
