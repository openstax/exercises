<% # Clients of this partial must supply the following variable:
   # answer %>

<div class="answer">
  <table>
    <tr>
      <td><%= answer.left_content_html.html_safe %></td>
      <td><p>-</p></td>
      <td><%= answer.right_content_html.html_safe %></td>
      <td><p>(<%= answer.is_true %><% if !answer.credit.nil? && answer.credit > 0 %> - <%= answer.credit %> pt<% end %>)</p></td>
    </tr>
  </table>
</div>
