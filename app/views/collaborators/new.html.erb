<%= page_heading 'Add collaborator' %>

<%= render :partial => 'shared/users_search',
           :locals => { :current_path => new_polymorphic_path([@publishable, :collaborator]),
                        :container => @publishable,
                        :presence_check_method => 'has_collaborator?',
                        :already_present_text => 'Already a collaborator',
                        :form_action_path => polymorphic_path([@publishable, :collaborators]),
                        :submit_text => 'Add collaborator' } %>

<br/>

<%= link_to 'Back', @publishable %>
