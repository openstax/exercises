<%= form_for(@attachable_asset) do |f| %>
  <% if @attachable_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachable_asset.errors.count, "error") %> prohibited this attachable_asset from being saved:</h2>

      <ul>
      <% @attachable_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attachable_id %><br />
    <%= f.number_field :attachable_id %>
  </div>
  <div class="field">
    <%= f.label :asset_id %><br />
    <%= f.number_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :local_name %><br />
    <%= f.text_field :local_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :attachable_type %><br />
    <%= f.text_field :attachable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
